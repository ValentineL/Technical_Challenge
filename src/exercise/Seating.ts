type SeatingExerciseMethodType = (input: Array<0 | 1>) => number[];
/**
 * Return all socially distanced seat positions until the room is full.
 * @return number[]
 */
const CovidSeating: SeatingExerciseMethodType = (input) => {
  //TODO add your solution here
  console.log(input);
  return [0];
};

export default CovidSeating;
